import 'package:flutter/material.dart';

class PrayerGridScreen extends StatelessWidget {
  const PrayerGridScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Prayer Grid Screen')),
    );
  }
}
